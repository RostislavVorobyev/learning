'use strict';

function calcAge(bitrhYear) {
  const age = 2037 - bitrhYear;
  console.log(firstName);

  function printAge() {
    const output = `you are ${age} born in ${bitrhYear}`;
  }
  return age;
}

calcAge(1991);
const firstName = 'Jonas';
